### UNIKERNEL LINUX

# To build your application with UKL, you just need to build it (not link) with two flags, i.e., -mno-red-zone -mcmodel=kernel. Then just build Linux and it will compile your unikernel.
