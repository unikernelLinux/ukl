/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf64-x86-64)
GROUP ( @abs_top_builddir@/libc.so.6 @abs_top_builddir@/libc_nonshared.a  AS_NEEDED ( @abs_top_builddir@/ld-linux-x86-64.so.2 ) )
