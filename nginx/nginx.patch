diff -upr nginx.orig/src/core/nginx.c nginx/src/core/nginx.c
--- nginx.orig/src/core/nginx.c	2022-05-23 19:59:19.000000000 -0400
+++ nginx/src/core/nginx.c	2022-07-01 15:42:59.506082499 -0400
@@ -208,9 +208,9 @@ main(int argc, char *const *argv)
         return 1;
     }
 
-    if (ngx_get_options(argc, argv) != NGX_OK) {
-        return 1;
-    }
+    // if (ngx_get_options(argc, argv) != NGX_OK) {
+    //     return 1;
+    // }
 
     if (ngx_show_version) {
         ngx_show_version_info();
diff -upr nginx.orig/src/os/unix/ngx_daemon.c nginx/src/os/unix/ngx_daemon.c
--- nginx.orig/src/os/unix/ngx_daemon.c	2022-05-23 19:59:19.000000000 -0400
+++ nginx/src/os/unix/ngx_daemon.c	2022-07-05 12:12:35.269775069 -0400
@@ -14,18 +14,6 @@ ngx_daemon(ngx_log_t *log)
 {
     int  fd;
 
-    switch (fork()) {
-    case -1:
-        ngx_log_error(NGX_LOG_EMERG, log, ngx_errno, "fork() failed");
-        return NGX_ERROR;
-
-    case 0:
-        break;
-
-    default:
-        exit(0);
-    }
-
     ngx_parent = ngx_pid;
     ngx_pid = ngx_getpid();
 
